export enum Role {
  ADMIN = 'admin',
  LEADER = 'leader',
  MEMBER = 'member',
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in_progress',
  DONE = 'done',
}
